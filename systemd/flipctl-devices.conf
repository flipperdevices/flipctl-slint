# Drivers whose device classes flipctl.service allows by name. systemd resolves a
# DeviceAllow=char-<name> against /proc/devices when the unit starts and silently
# drops a name that is not registered yet, so the drivers are loaded first rather
# than on the first plug-in.
usbserial
cdc-acm
spidev
